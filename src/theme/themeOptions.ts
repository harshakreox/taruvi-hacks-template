// Re-export the active theme defined at the project root so callers using
// the legacy `src/theme/themeOptions` path stay in sync with the design system.
export { lightThemeOptions, darkThemeOptions, taruviTokens } from "../../themeOptions";
