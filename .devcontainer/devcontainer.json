{
  "name": "taruvi-refine-template",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:1-22-bullseye",
  "hostRequirements": {
    "cpus": 4,
    "memory": "16gb",
    "storage": "32gb"
  },
  "remoteEnv": {
    "CODEX_HOME": "${containerWorkspaceFolder}/.codex"
  },
  "postCreateCommand": "bash -lc '[ -f .mcp.json ] || cp .mcp.example.json .mcp.json; [ -f .env ] || cp .env.example .env; mkdir -p .codex/projects; source /usr/local/share/nvm/nvm.sh; nvm install 22; nvm use 22; npm install --legacy-peer-deps; command -v codex >/dev/null 2>&1 || npm install -g @openai/codex'",
  "postStartCommand": "bash -lc '[ -f .mcp.json ] || cp .mcp.example.json .mcp.json; [ -f .env ] || cp .env.example .env; mkdir -p .codex/projects'",
  "postAttachCommand": {
    "app": "bash -lc 'source /usr/local/share/nvm/nvm.sh; nvm install 22; nvm use 22; [ -d node_modules ] || npm install --legacy-peer-deps; npm run dev -- --host 0.0.0.0 --port 5173'",
    "openEnv": "sleep 4 && code .env"
  },
  "forwardPorts": [5173],
  "portsAttributes": {
    "5173": {
      "label": "Refine Dev Server",
      "onAutoForward": "openPreview"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "seunlanlege.action-buttons",
        "openai.chatgpt"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash",
        "github.copilot.enable": { "*": false },
        "github.copilot.editor.enableAutoCompletions": false,
        "github.copilot-chat.welcomeMessage": "never",
        "extensions.disabled": ["GitHub.copilot-chat", "GitHub.copilot"],
        "chat.disableAIFeatures": true,
        "files.exclude": {
          "node_modules/": true,
          ".codex/": true
        },
        "editor.minimap.enabled": false,
        "actionButtons": {
          "defaultColor": "#ffffff",
          "loadNpmCommands": false,
          "commands": [
            {
              "name": "⚙️ Setup .env",
              "singleInstance": true,
              "command": "code .env",
              "tooltip": "Open .env and fill in your Taruvi credentials (copy from the onboarding page)"
            },
            {
              "name": "🔑 Connect Codex",
              "singleInstance": true,
              "command": "bash scripts/start-codex-devcontainer.sh",
              "tooltip": "Fetch your AI provider key from Taruvi, configure MCP, and open the Codex sidebar — your default way to build"
            },
            {
              "name": "👁️ Open Preview",
              "singleInstance": false,
              "command": "bash scripts/open-preview.sh",
              "tooltip": "Print the preview URL — opens in a new browser tab. Log in there first, then the Simple Browser in VS Code will reflect your session."
            },
            {
              "name": "🤖 Codex CLI",
              "singleInstance": true,
              "command": "bash -c 'set -a; [ -f .env ] && source .env; set +a; exec codex'",
              "tooltip": "Optional: launch Codex in the terminal instead of the sidebar — run 🔑 Connect Codex first"
            }
          ]
        }
      }
    }
  }
}
